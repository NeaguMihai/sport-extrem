spring:
  jpa:
    show-sql: false
    hibernate:
      ddl-auto: validate
  datasource:
    initialization-mode: never
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3308/atta_problem
    username: atta_admin
    password: mockpassword
